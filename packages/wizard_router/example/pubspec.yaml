name: wizard_router_example
description: Demonstrates how to use the wizard_router package.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.0
  provider: ^6.0.0
  wizard_router: ^1.2.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
