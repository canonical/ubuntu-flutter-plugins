name: ubuntu_flutter_plugins_workspace

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  melos: ^4.1.0

command:
  bootstrap:
    environment:
      sdk: ">=3.0.0 <4.0.0"
      flutter: '>=3.13.0'

    dependencies:
      collection: ^1.17.0
      yaru: ^4.0.0

    dev_dependencies:
      ubuntu_lints: ^0.3.0
      yaru_test: ^0.1.6
